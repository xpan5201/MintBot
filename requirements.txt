# MintChat Requirements
# Python 3.12 (测试阶段仅支持 3.12，确保生态稳定性和安全性)
# 更新日期: 2025-11-19 (v2.54.1)
# 版本策略: 使用2025年最新稳定版本，确保兼容性和性能

# LangChain Core - 2025最新稳定版 (v2.30.12更新)
langchain>=1.0.7  # 2025-11-14最新版，性能优化
langchain-core>=1.0.5  # 最新稳定版
langchain-community>=0.4.1
langgraph>=1.0.0

# LLM Providers - 最新版本 (v2.30.12更新)
langchain-openai>=1.0.3  # 最新版本
langchain-anthropic>=1.0.2
langchain-google-genai>=3.0.1
openai>=2.7.1

# Multimodal Processing - 优化版本
pillow>=11.0.0
opencv-python>=4.10.0
soundfile>=0.12.1
pydub>=0.25.1
librosa>=0.10.2
pytesseract>=0.3.13
pygame>=2.6.1  # v2.52.0: 音频播放器（替代PyQt6 QMediaPlayer）

# Vector Databases - 最新稳定版 (v2.30.12更新)
chromadb>=1.3.4,<2.0.0  # 最新稳定版，等待2.0正式版
langchain-chroma>=1.0.0
faiss-cpu>=1.9.0

# Embeddings - 性能优化版本 (5.x性能提升30%+)
sentence-transformers>=5.1.2

# Search & Retrieval - v2.30.40 新增
rank-bm25>=0.2.2  # BM25 关键词检索算法

# Graph & Network - v2.30.43 新增
networkx>=3.4.2  # 知识图谱和网络分析

# Cache & Performance - v2.30.44 新增
redis>=5.0.0  # Redis 缓存（可选，用于多级缓存系统）

# Configuration & Validation - 最新版本 (v2.30.12更新)
pydantic>=2.12.4  # V2性能优化，验证速度提升5-50倍
pydantic-settings>=2.11.0
python-dotenv>=1.0.1
pyyaml>=6.0.2

# HTTP & Async - 性能优化 (v2.30.12更新)
requests>=2.32.3
aiohttp>=3.13.2  # 最新版本，异步性能优化
httpx>=0.27.0  # TTS 客户端异步 HTTP 请求库（必需）

# Logging - 最新版本
loguru>=0.7.3

# System Monitoring - v2.30.6 新增
psutil>=6.1.0

# MCP (Model Context Protocol) - v2.30.15 新增
mcp>=1.21.1  # MCP Python SDK
mcp-server-time>=2025.9.25  # 时间MCP服务器
starlette>=0.50.0  # MCP依赖
sse-starlette>=3.0.3  # SSE支持
pywin32>=311  # Windows平台支持

# GUI (PyQt6) - 最新稳定版
PyQt6>=6.8.0

# LangSmith (Optional) - 最新版本
langsmith>=0.2.11

# Development Tools (Optional)
pytest>=8.3.4
black>=24.10.0
flake8>=7.1.1
mypy>=1.13.0

# Development Dependencies (install with: pip install -e ".[dev]")
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0
# ipython>=8.12.0
# jupyter>=1.0.0
# pytest-cov>=4.1.0

